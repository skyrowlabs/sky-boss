{
  "name": "sky-boss-shell",
  "private": true,
  "version": "0.0.1",
  "description": "The window. An Electron host for the canvas sb already serves — and, in main.js, the OS-window model it was measured against.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "start:canvas": "electron canvas-main.js"
  },
  "devDependencies": {
    "electron": "^44.0.0"
  }
}
