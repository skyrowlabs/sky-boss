# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Secrets — never committed
.env

# Scratch
tmp/

# Run state lives in ~/.local/state/sb/, not here (see CLAUDE.md § CLI setup).
# This is a backstop for strays only.
logs/

# Operator content lives in $SB_HOME (~/.sky-boss), never here.
# Backstop against a stray recommit.
inventory/
jobs/
watches/

# Operator context for this machine — never committed.
# The published half is CLAUDE.md.
CLAUDE.local.md

# Node — lint tooling only; the project is Python.
node_modules/

# ── Added by the skeletor scaffold, 2026-09-06 ──
.env.*
!.env.example
!.env.ci
*.pem
*.key
.venv-ci/
venv/
.mypy_cache/
.coverage
coverage.xml
htmlcov/
*.egg-info/
dist/
build/
npm-debug.log*
.DS_Store
.idea/
*.swp
.claude/settings.local.json
.claude/worktrees/
