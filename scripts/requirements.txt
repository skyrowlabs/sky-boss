# Host-side toolchain: the CLI, the docs pipeline, and the lint gates.
#
# Lint tool versions MUST match .pre-commit-config.yaml. Neither file is
# canonical: Dependabot bumps this one and cannot touch the other, so a
# mismatch means make them agree, at whichever version you mean.
# tests/test_lint_tool_parity.py fails if they diverge — two versions of a
# formatter disagree about real formatting, and a local pass with a CI failure
# on the same file is otherwise unexplainable.

click>=8.5.0

pytest>=9.1.1
pytest-timeout>=2.4.0
pytest-cov>=7.1.0

flake8==7.3.0
black==26.5.1
isort==9.0.1
pyright==1.1.414
pre-commit>=4.6.2
