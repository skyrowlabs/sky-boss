# Architecture

<!-- SCAFFOLD: this file is the map an agent reads first. Fill it in before the
     project has enough code to need it — a stub here is cheaper than an
     archaeology session later. -->

## What This Is

One CLI for watching what other tools do, here and across these repos

## Components

| Component | Responsibility | Talks to |
| --------- | -------------- | -------- |
|           |                |          |

## Data Flow

```
<the one diagram that explains the system>
```

## Critical Patterns

Patterns here are ones a change can violate **silently** — where the code still
runs and the failure surfaces somewhere else. Each entry says what the pattern
is, and what breaks without it.

### <Pattern name>

What it is. What goes wrong when it is not followed, concretely.

## Decisions and Their Reasons

For anything a future reader would otherwise re-litigate, link the plan in
`docs/implementations/` that recorded the decision. The archive's `⭐⭐⭐` entries
are the ones to read before modifying the system they describe.
