---
name: Feature request
about: Something sky.boss should be able to do
labels: enhancement
---

## What you are trying to do

<!-- The task, not the feature. What are you watching, and with what today? -->

## Why the existing commands do not do it

<!-- `sb run`, `read`, `data`, `follow`, `roll-call`, `schedule`, `tools`, `ui`.
     Wrapping an external CLI for passthrough is deliberately rejected —
     `sb gh pr list` is strictly worse than `gh pr list`. See CLAUDE.md § Scope
     for what else has been considered and declined, so a proposal can argue
     with the reasoning rather than rediscover it. -->

## Is it a read or a write?

<!-- `sb run` is the only command that acts. A proposal that both reads and
     writes is two proposals. -->
